# Implementation Plans

Generated by the improve skill on 2026-06-10 against commit `cd2908e` (master).
Reconciled 2026-07-21: HEAD is still `cd2908e` (zero source commits since the
audit — every finding below remains current). Plans 009–013 were added that
day by promoting backlog findings; no re-audit was performed. New since June:
`npm audit` now reports 3 high + 1 moderate advisories (`vite`, `ws` via
`engine.io-client`, `brace-expansion`) — all in the dev-tooling chain, none
shipped to consumers, all fixed by the `npm audit fix` step already in
plan 007.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

> **Selection note**: this run was non-interactive, so plans were written for
> the top findings by leverage (impact ÷ effort, weighted by confidence) per
> the improve skill's default. Findings that were vetted but not planned are
> preserved in the Backlog section so a future session can promote them
> without re-auditing.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fix three small correctness/perf bugs in the prompt-budget path | P1 | S | — | TODO |
| 002  | Make the models.dev metadata fetch unable to hang or fail a commit run | P1 | S | — | TODO |
| 008  | Remove provider aliases — one canonical id per provider, aligned with models.dev | P2 | M | — | TODO |
| 003  | Add unit tests for the untested LLM pure layer (providers, output, prompt) | P1 | M | — | TODO |
| 004  | Lazy-load provider SDKs so a run only pays for the provider it uses | P2 | M | 003 | TODO |
| 005  | Keep API keys out of process argv (keychain writes and `auth login --token`) | P2 | M | — | TODO |
| 006  | Harden the commit-message editor flow (cleanup, fallbacks, honest failures) | P2 | M | — | TODO |
| 007  | CI matrix, audit gate, and a fast local test loop | P2 | S | — | TODO |
| 013  | Warn when repo-supplied config redirects prompts/credentials to a custom endpoint | P2 | M | not concurrent with 001 | TODO |
| 012  | Classify exit codes with typed errors instead of message regexes | P2 | M | after 004 + 008 | TODO |
| 009  | Make README and AGENTS.md stop claiming things the code doesn't do | P2 | S | after 005 + 008 (README) | TODO |
| 010  | Delete the orphaned root entrypoint and dead config-command code | P3 | S | — | TODO |
| 011  | Enforce numeric ranges in validateConfig (temperature, cacheTtlHours) | P3 | S | not concurrent with 009 (README) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

(Table order is the recommended execution order; 008 sits before 003 deliberately —
see dependency notes.)

## Dependency notes

- **Recommended: 008 before 003** — 008 deletes the alias tables and renames three canonical ids; running it first means 003's tests pin the final no-alias world instead of pinning behavior that 008 immediately deletes. Both plans carry annotations for either order; they must not run concurrently (both touch `src/llm/providers.test.ts`).
- **004 requires 003**: 004 rewrites `src/llm/providers.ts` to dynamic imports; 003's provider-id tests are the only thing that proves the refactor preserved behavior. Do not run 004 first. 004 is also annotated for running before/after 008 (different aspects of the same factory table: 008 renames keys, 004 makes values lazy).
- 001/002/005/006/007 are mutually independent and parallelizable (no shared files).
- 003, 004, and 008 all touch `src/llm/providers.ts` and/or `src/llm/providers.test.ts` — run them sequentially, never concurrently.
- **012 after 004 and 008**: 012 touches `src/llm/generate.ts` (shared with 004) and deliberately leaves `providers.ts`'s plain errors regex-classified because 004/008 own that file; running it last avoids rebase churn.
- **013 not concurrent with 001**: both edit `src/commands/default.ts`.
- **README.md is touched by 005, 008, 009, and 011** — run those four sequentially in any order that puts 009 after 005 and 008 (009 rewords the auth section 005 edits, and must not re-document aliases 008 deletes).
- 010 is fully independent of everything.

## Backlog — vetted findings, not planned this round

(Each was confirmed against the code at `cd2908e`; promote to a numbered plan when picked up.)

- ~~**Provider alias registry divergence**~~ — **promoted to plan 008** (2026-06-10, maintainer decision). Approach changed from *consolidate into one registry* to *remove aliases entirely*, with canonical ids aligned to models.dev's keys (`bedrock`→`amazon-bedrock`, `vertex`→`google-vertex`, `vertex-anthropic`→`google-vertex-anthropic`) so the single surviving name resolves the provider, auth, AND metadata.
- ~~**Project-config trust boundary**~~ — **promoted to plan 013** (2026-07-21). Warn-only mitigation per the recorded policy: stderr notice when project/inline config selects an `openai-compatible` endpoint, plus a cleartext notice for non-loopback `http:` baseUrls. Blocking/trust-store mechanics explicitly deferred.
- ~~**Docs truth pass**~~ — **promoted to plan 009** (2026-07-21). Correction found during promotion: item (d) was half wrong — `ZENCOMMIT_CONFIG_CONTENT` IS documented (README:176-178); only `ZENCOMMIT_MOCK_RESPONSE` is undocumented, and the plan documents it in AGENTS.md (contributor-facing), not README.
- ~~**Dead/vestigial code sweep**~~ — **promoted to plan 010** (2026-07-21), minus the `PROVIDER_ALIASES` identity entries, which plan 008 supersedes (it deletes the whole table); 010 explicitly excludes them.
- ~~**`validateConfig` range gaps**~~ — **promoted to plan 011** (2026-07-21). Bound decisions: temperature 0–2 inclusive (README's "0-1" row updated to match), cacheTtlHours finite and ≥ 0.
- ~~**Error classification fragility**~~ — **promoted to plan 012** (2026-07-21). Scope decision: typed classes for `generate.ts`'s throws in a new leaf `src/llm/errors.ts`; `providers.ts`'s plain errors stay regex-classified until 004/008 settle (file contention); regex branches kept as SDK-error fallback.
- **`getFileSummary` rename-path matching** (correctness, S, LOW): numstat↔name-status path joining for renames relies on three fallback map lookups (`src/git/diff.ts:78-92`); renames with tabs/odd formats can show `(+? -?)`. Cosmetic. **Deliberately not planned** (2026-07-21) — revisit only if a rename-display bug is actually reported.

## Direction options (maintainer decisions, not defects)

Grounded in repo evidence; effort estimates are coarse:

1. **`prepare-commit-msg` hook integration** (`zencommit hooks install`) — all building blocks exist: non-interactive `--yes`, `--dry-run`, exit-code contract, staged-diff default. The classic adjacent feature for commit-message generators; would need a `--batch` audit of every interactive path. Effort ~M.
2. **Cross-platform secret store** — `SecretStore` is already an interface with an injectable implementation (`src/auth/secrets.ts:159-163`); adding libsecret (Linux) / Credential Manager (Windows) backends, or an encrypted-file fallback, closes the README claim gap for non-mac users of a public npm package. Effort M–L.
3. **Expose gitmoji via CLI** — a complete `src/llm/prompts/gitmoji.md` exists and is wired to `commit.emoji` config (`src/llm/prompt.ts:65`), but no `--emoji` flag exists (`--format` only offers conventional|freeform). One yargs option + `applyCliOverrides` line. Effort S.
4. **PR-description / changelog generation** — the diff→truncate→prompt pipeline (`truncate.ts`, `tokens.ts`, `prompt.ts`) is commit-message-agnostic; a second command reusing it with different templates is cheap relative to its surface value. Spike first. Effort M.

## Findings considered and rejected

(So nobody re-audits them.)

- **"Default model `openai/gpt-5` does not exist" (audit DOCS-03)**: false — `gpt-5` exists in the models.dev dataset (verified in the local cache); the default config is functional.
- **"Metadata fallback is silent" (audit CORRECTNESS-09)**: false — `src/commands/default.ts:121-122` emits `console.warn('Model metadata not found. Using conservative token limits.')`.
- **Commit-subject argument injection via leading `-` (audit SECURITY-02)**: not exploitable — the subject is the argv value consumed by `-m` (`git` parse-options takes the next argv element as the option's value regardless of leading dash); `spawn` without a shell prevents the rest.
- **Verbose logs print the diff/prompt at `-vvv` (audit SECURITY-01)**: by design — explicit opt-in debug verbosity on a local tool printing the user's own diff to stderr. Not a defect.
- **Secrets injected into `process.env` inherited by children (audit SECURITY-04)**: by design — env vars are the AI SDKs' documented credential channel; children spawned are git and the user's editor.
- **Keychain account names reveal configured providers (audit SECURITY-09)**: accepted trade-off; the auditor itself concluded no action needed.
- **`metadata.providers.local.path` path traversal (audit SECURITY-06)**: config is user/repo-controlled and the file content is only parsed as model metadata, never echoed or exfiltrated; reading files is what config does. No action.
- **Zod/schema-based config validation rewrite (audit DEBT-04)**: not worth it — the hand-rolled validator is 136 lines, complete, and tested; a library migration adds a dependency for no behavior gain. (Range checks are tracked in backlog instead.)
- **Truncation strategy dispatch consolidation (audit DEBT-05), `maxFiles` enforcement location (DEBT-07)**: cosmetic; cost of churn exceeds benefit.
- **Pre-commit hooks (audit DX-02)**: rejected for now — single-maintainer repo, CI is the enforcement point; revisit if contributor count grows.
- **`security find-generic-password` per key in `auth status` (~24 sequential spawns)**: cosmetic latency in a rarely-run command.

## Audit coverage note

Audited at `standard+` depth (six parallel read-only auditors, whole `src/`, `tests/`, `scripts/`, CI):
correctness, security, performance, tests, debt, dependencies, DX, docs, direction.
Not audited: `dist/` (generated), `package-lock.json` internals beyond the `ws` advisory chain,
the `release.yml` publishing flow's npm-side configuration, and the upstream `gitlab-ai-provider`
package internals (black-box). Startup timing was measured on the advisor's machine (macOS, arm64);
re-measure locally in plan 004.
